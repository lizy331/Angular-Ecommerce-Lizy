import { Lizy2ShopValidators } from './lizy2-shop-validators';

describe('Lizy2ShopValidators', () => {
  it('should create an instance', () => {
    expect(new Lizy2ShopValidators()).toBeTruthy();
  });
});
